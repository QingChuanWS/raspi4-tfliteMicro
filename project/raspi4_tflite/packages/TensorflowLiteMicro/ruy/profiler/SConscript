from building import *

cwd     = GetCurrentDir()
src     = Glob('*.c') + Glob('*.cc')
CPPPATH = [cwd, str(Dir('#'))]

group = DefineGroup('ruy', src, depend = ['BSP_USING_TENSORFLOWLITEMICRO'], CPPPATH = CPPPATH)

Return('group')
