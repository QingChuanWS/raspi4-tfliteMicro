from building import *

cwd     = GetCurrentDir()
src     = Glob('*.c') + Glob('*.cc')
CPPPATH = [cwd, str(Dir('#'))]

group = DefineGroup('Flatbuffers', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
